// Simple configuration for Gemini API
// In a real app, this would contain API keys and other configuration

const geminiConfig = {
  apiKey: 'dummy-api-key', // This is a placeholder
  model: 'gemini-pro',
  maxTokens: 1024,
  temperature: 0.7,
};

export default geminiConfig;